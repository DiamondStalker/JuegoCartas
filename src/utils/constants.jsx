const SERVER_IP = "deckofcardsapi.com";

export const ENV = {
    BASE_PATH: `https://${SERVER_IP}`,
    BASE_API: `https://${SERVER_IP}/api/deck`,
    API_ROUTES: {
        NEW_GAME : "new/shuffle/?deck_count=1"
    }
}

